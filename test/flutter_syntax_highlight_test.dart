import 'package:flutter_test/flutter_test.dart';

void main() {
  // **자리를 채우는 테스트다. 아무것도 주장하지 않는다.**
  //
  // 이 단계의 패키지에는 동작이 없으므로 검사할 동작도 없다. 그런데
  // `flutter test`는 테스트 파일이 하나도 없으면 종료 코드 1로 죽으므로
  // (측정함), 게이트를 돌리려면 파일이 하나는 있어야 한다.
  //
  // 라이브러리를 import해서 "컴파일된다"를 주장하려 했으나 그만두었다.
  // 아직 내보내는 것이 없어 그 import는 진짜로 쓸모가 없고 `flutter analyze`가
  // 정확히 그렇게 신고했다. 그리고 라이브러리가 컴파일된다는 것은 `analyze`가
  // 이미 증명한다 — 여기서 다시 주장할 필요가 없다.
  //
  // 토크나이저가 들어오면서 이 파일은 진짜 테스트로 대체된다. 그때까지 이것은
  // 실패할 수 없는 게이트이며, 그 사실을 숨기지 않는다.
  test('자리표시 — 게이트가 돌 수 있게 한다', () {
    expect(true, isTrue);
  });
}
